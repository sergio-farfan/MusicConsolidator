# Read-only library evidence (125MB each — exceeds GitHub's 100MB file limit; local only)
Library*.xml

# Build products (rebuild via macos-app/scripts/build-app.sh)
macos-app/ConsolidatorKit/.build/
macos-app/build/

# SDD scratch workspace (durable records live under docs/superpowers/)
.superpowers/

.DS_Store

# DMG installers (rebuild via macos-app/scripts/package-dmg.sh)
macos-app/dist/

# Local-only records and scratch evidence (2026-08-11): plan/run artifacts,
# workflow ledgers, and analysis exports stay on disk, out of the repo.
reports/
docs/superpowers/
/*.csv
/current_duplicate_playlists.md
/soundiiz_merge_audit_musica_xtotal.md

# Python development reference (2026-08-11): engine reference, CLI, and its
# suite stay local-only; the Swift app is the repository's implementation.
apple_music_consolidator/
scripts/
tests/

# Operating contract and workflow docs (2026-08-12): local-only.
/AGENTS.md
/CLAUDE.md
docs/apple-music-consolidator.md
